* 根节点黑色
* 红节点的子节点是黑色
* 每条路径黑色节点数目一致

公式：
1. 
* 如果是根节点标为黑色

2. 
* 新插入节点标记红色
* 如果父节点是红色，父节点的兄弟节点是红色的
* 将父节点和父节点的兄弟节点标记为黑色
* 将新插入节点和其祖父节点标为红色
* 将根节点标位黑色

3.
* 将新插入节点表为红色
* 如果父亲节点是红色，父亲节点的兄弟节点是黑色
* 需要旋转
* * 左左：右转根节点
* * 左右：走转父节点，然后执行左左
* * 右右：旋转根节点
* * 右左：右转父节点，然后执行右右
* 变色

